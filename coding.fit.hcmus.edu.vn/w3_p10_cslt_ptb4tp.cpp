#include <bits/stdc++.h>

using namespace std;

int main() {
    double a, b, c;

    double delta = b * b - 4 * a * c;

    if (delta < 0) {
        cout << "NO SOLUTION";
    } else {
        if (delta == 0) {
            printf("x1 = %.2f, x2 = %.2f",);
        } else {

        }
    }

    return 0;
}
