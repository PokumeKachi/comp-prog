#include <cstdio>
#include <iostream>
#include <string>


int main() {
    std::ios_base::sync_with_stdio(0);
    std::cin.tie(nullptr);

    int t;

    std::cin >> t;

    std::string res = "";

    while (t--) {
        int n, m, k;
        std::cin >> n >> m >> k;

        for (int i = 0; i < m; ++i) {

        }

        res += '\n';
    }

    std::cout << res;

    return 0;
}
