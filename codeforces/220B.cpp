#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int n, m;
    cin >> n >> m;

    int a[100'005];

    for (int i = 0; i < n; ++i) {
        cin >> a[i];
    }

    int l, r;

    for (int i = 0 ; i < m; ++i) {
        cin >> l >> r;
    }


    return 0;
}
