#include <stdio.h>

#include <cstdint>
#include <utility>
#include <vector>

#define SIZE 200005

int t, n, k;
int i, j;
int res;

std::vector<std::pair<std::uint32_t, std::uint32_t>> TREE();

int main() {
    scanf("%d%d", &t);

    while (t--) {
    }

    return 0;
}
